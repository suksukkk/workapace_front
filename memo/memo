*********인라인요소 - span, label, input, button, td, a
******** 블록요소  - h1~6, div, ul, ol, li, table, tr, p
**인라인블록요소 - form관련태그, img


span, label 등의 기본 display 속성은 inline이다.
(width 값이 적용안됨 width값을 적용하려면 display: inline-block;으로 변경해야함)


ul, ol :

ul은 순서 없는 list
ol은 순서 있는 list
ul, ol에서 text-align center를 할 경우 li요소들이 가운데 정렬 됨.


margin  : 요소의 바깥에서 밀어냄
padding : 요소의 안쪽에서 늘림

