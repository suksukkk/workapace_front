span, label 등의 기본 display 속성은 inline이다.(width 값이 적용안됨 width값을 적용하려면 display: inline-block;으로 변경해야함)


ul, ol :

ul은 순서 없는 list
ol은 순서 있는 list
ul, ol에서 text-align center를 할 경우 li요소들이 가운데 정렬 됨.


margin  : 요소의 바깥에서 밀어냄
padding : 요소의 안쪽에서 늘림

